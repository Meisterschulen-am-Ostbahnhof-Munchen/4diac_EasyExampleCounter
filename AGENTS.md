# Instructions for Agents

- **No Web Searches**: Do NOT use the `search_web` or `read_url_content` tools to search the web.
- **XSD Schema Files**: If you need additional XSD schema files, ask the user directly.
